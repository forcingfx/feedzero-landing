<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>FeedZero Release Notes</title>
  <subtitle>What changed in FeedZero.</subtitle>
  <id>feedzero:changelog</id>
  <updated>2026-05-15T12:00:00Z</updated>
  <link rel="self" href="https://feedzero.app/releases.xml" />
  <link rel="alternate" type="text/html" href="https://feedzero.app/#releases" />
  <author>
    <name>FeedZero</name>
  </author>
  <entry>
    <id>feedzero:release:0.8.2</id>
    <title>v0.8.2 — Article flood grouping, cloud restore, and sync race fix</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-05-15T12:00:00Z</published>
    <updated>2026-05-15T12:00:00Z</updated>
    <summary>Flooding feeds no longer drown out everything else in aggregated views, a Restore-from-cloud button recovers from drifted local state, and a cross-device sync race that left Device B with the wrong feed list is fixed.</summary>
    <content type="html"><![CDATA[<p>Flooding feeds no longer drown out everything else in aggregated views, a Restore-from-cloud button recovers from drifted local state, and a cross-device sync race that left Device B with the wrong feed list is fixed.</p>
<h3>Added</h3>
<ul>
  <li>Added inline grouping for same-feed article floods in aggregated views (All items and folder views). Runs of three or more consecutive articles from the same feed within ten-minute pairwise gaps collapse to a summary row with a chevron; expanding inline reveals the full list. Disabled in per-feed views and behind a Settings toggle if undesired.</li>
  <li>Added a <code>Restore from cloud</code> button to the Data &amp; Storage dialog. Replaces local feeds and articles with the cloud vault without deleting and recreating the vault, which is the recovery path when a device's local state drifts from what the cloud knows.</li>
</ul>
<h3>Fixed</h3>
<ul>
  <li>Fixed a cross-device sync race where a second device could mark its status as synced without the cloud feeds materializing locally. Concurrent pulls now share a single in-flight request and the import is transactional, so the article list never serves a half-imported state.</li>
  <li>Fixed the prev/next reader navigation pills appearing on desktop, where they duplicated existing keyboard shortcuts and sidebar navigation. The pills remain on mobile, where the touch surface is needed.</li>
  <li>Fixed dialog buttons being pushed off-screen on iOS when the soft keyboard opens. Dialogs are now anchored to the dynamic viewport height so the action row stays reachable regardless of keyboard state.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
  <entry>
    <id>feedzero:release:0.8.1</id>
    <title>v0.8.1 — Server reliability, observability, and rate limiting</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-05-14T13:00:00Z</published>
    <updated>2026-05-14T13:00:00Z</updated>
    <summary>Three production bugs fixed across cloud sync, the public stats page, and mobile dialogs. Server-side storage consolidated onto a single backend. Every error response now carries a trace identifier for support.</summary>
    <content type="html"><![CDATA[<p>Three production bugs fixed across cloud sync, the public stats page, and mobile dialogs. Server-side storage consolidated onto a single backend. Every error response now carries a trace identifier for support.</p>
<h3>Added</h3>
<ul>
  <li>Added per-client rate limiting on the feed and page proxies. Default 300 requests per minute per client; returns <code>429 Retry-After</code> per RFC 6585 when exceeded.</li>
  <li>Added a trace identifier (<code>req_</code> followed by 8 hex characters) to every non-2xx response from the monetization endpoints. Quote it in a support report and the issue can be looked up in the runtime logs.</li>
  <li>Added structured single-line JSON logs on every 5xx server error, with an allow-listed field set (route, method, status, trace identifier, error class, error message). No personal data is ever logged.</li>
  <li>Added module-load logs in each serverless function that surface which storage backend resolved at cold start. Any future regression where the wrong adapter is chosen becomes visible in the first deploy log.</li>
  <li>Added production smoke tests for every server endpoint. Run with <code>SMOKE_TESTS=1 npx vitest run tests/smoke/</code>. The smoke layer is now a required phase of the standard development workflow.</li>
</ul>
<h3>Changed</h3>
<ul>
  <li>Consolidated all server-side persistence (license records, vault sync, Stripe event deduplication, the feed catalog, and rate-limit counters) onto a single Upstash KV instance. Replaces a previous mix of Vercel Blob, separate Upstash, and in-memory adapters. Self-hosters not configuring Upstash continue to use the filesystem and memory adapters.</li>
</ul>
<h3>Fixed</h3>
<ul>
  <li>Fixed cloud sync vault pulls returning the literal string <code>[object Object]</code> instead of the encrypted payload. The server was storing vaults correctly all along; only the read path was broken. Existing vaults are unaffected and now load correctly.</li>
  <li>Fixed cloud sync pushes failing with a 500 error after a deployment-time configuration drift. Sync storage now self-detects the correct backend rather than relying on an exact environment-variable match.</li>
  <li>Fixed the public stats page always showing zero feeds tracked. The feed catalog had no persistence layer and reset on every cold start; now backed by persistent storage so counts and the popular-feeds leaderboard populate correctly.</li>
  <li>Fixed the mobile soft keyboard covering the bottom of every dialog, which made the cloud-sync passphrase entry unusable. The fix applies to every dialog in the app.</li>
  <li>Fixed the mobile bottom navigation drawer being partially hidden behind iOS Safari's browser chrome. The drawer's bottom padding now grows dynamically with the toolbar height.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
  <entry>
    <id>feedzero:release:0.8.0</id>
    <title>v0.8.0 — First-launch reliability fix</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-05-09T12:00:00Z</published>
    <updated>2026-05-09T12:00:00Z</updated>
    <summary>A latent bug in the new-user initialization path that could leave the app marked as onboarded after a failed first init is fixed.</summary>
    <content type="html"><![CDATA[<p>A latent bug in the new-user initialization path that could leave the app marked as onboarded after a failed first init is fixed.</p>
<h3>Fixed</h3>
<ul>
  <li>Fixed first-launch initialization continuing to mark onboarding complete when the initial database setup failed (for example on browsers without Web Crypto, such as iOS Lockdown Mode). The error is now surfaced and onboarding is not marked complete, so the user is prompted to retry rather than landing in a half-initialized state.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
  <entry>
    <id>feedzero:release:0.7.0</id>
    <title>v0.7.0 — Mobile bottom drawer, pill navigation, and the stats page</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-05-03T12:00:00Z</published>
    <updated>2026-05-03T12:00:00Z</updated>
    <summary>A persistent swipeable bottom drawer replaces the offcanvas mobile sidebar, navigation pills handle prev/next on every device, and a public /stats dashboard exposes anonymous usage numbers.</summary>
    <content type="html"><![CDATA[<p>A persistent swipeable bottom drawer replaces the offcanvas mobile sidebar, navigation pills handle prev/next on every device, and a public /stats dashboard exposes anonymous usage numbers.</p>
<h3>Added</h3>
<ul>
  <li>Added a persistent swipeable bottom drawer on mobile that replaces the offcanvas sidebar. The drawer handle is always visible at the bottom of the screen and shows the current feed name; tapping or swiping up reveals the full feed list, folders, All items, Explore, and Settings.</li>
  <li>Added a bottom sheet feed switcher on mobile. The active feed name in the bottom drawer doubles as a switcher: a tap opens a sheet with all feeds and folders.</li>
  <li>Added a public stats page at <code>/stats</code>. Shows total vaults, total feeds tracked, and the top 100 feeds by request volume. No accounts, no personal data, no login.</li>
  <li>Added automatic color assignment for new folders. Created folders now get a distinct color from the eight-color palette without an extra picker step.</li>
  <li>Added the full settings menu inline in the mobile bottom drawer. Cloud sync, Auto-organize, Keyboard shortcuts, Send feedback, and What's new are all reachable without opening a separate dropdown.</li>
</ul>
<h3>Changed</h3>
<ul>
  <li>Replaced the pull-to-advance gesture on mobile with explicit prev/next nav pills. The pills are pinned to the bottom of the reader and remain accessible regardless of scroll position. Pull-to-advance was removed because behavior was inconsistent across mobile browsers.</li>
  <li>The mobile nav pills are now icon-only for back navigation, show no keyboard hints, and stretch to fill the available width. Long article titles in the prev/next labels are truncated with ellipses instead of overflowing.</li>
  <li>The desktop sidebar and the mobile bottom drawer now share a single navigation body component. Adding a new top-level entry (such as Explore) only needs to be done in one place.</li>
</ul>
<h3>Fixed</h3>
<ul>
  <li>Fixed the article list jumping unexpectedly when clicking an already-visible article. The virtualizer no longer re-anchors on click-driven selection changes; keyboard <kbd>j</kbd>/<kbd>k</kbd> still scroll the selected article into view.</li>
  <li>Fixed the mobile bottom drawer overflowing horizontally on viewports with safe-area insets. The drawer now respects the iOS Dynamic Island and notch in all orientations.</li>
  <li>Fixed desktop reader navigation pills clipping long article titles. The pills now share the available width and grow to fit instead of letting text overflow.</li>
  <li>Fixed the folder title toggle in the sidebar collapsing the folder when clicked. Selecting a feed inside a folder on mobile now snaps back to the article list as expected.</li>
  <li>Fixed the mobile drawer settings rendering as a nested dropdown that introduced horizontal scroll. The settings entries are now inline within the drawer.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
  <entry>
    <id>feedzero:release:0.6.0</id>
    <title>v0.6.0 — Resizable panels, navigation pills, and auto-organize</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-05-01T12:00:00Z</published>
    <updated>2026-05-01T12:00:00Z</updated>
    <summary>Three-panel resizable desktop layout, always-visible prev/next navigation, pull-to-advance on mobile, folder colors, and keyword-based auto-organize.</summary>
    <content type="html"><![CDATA[<p>Three-panel resizable desktop layout, always-visible prev/next navigation, pull-to-advance on mobile, folder colors, and keyword-based auto-organize.</p>
<h3>Added</h3>
<ul>
  <li>Added a three-panel resizable desktop layout. The sidebar, article list, and reader are now independently draggable columns.</li>
  <li>Added prev/next navigation pills pinned to the bottom of the reader panel. The pills are always visible regardless of scroll position and show <kbd>k</kbd>/<kbd>j</kbd> keyboard hints.</li>
  <li>Added pull-to-advance gesture on mobile. Scrolling past the end of an article advances to the next; pulling down from the top goes back.</li>
  <li>Added folder color customization. An eight-color swatch picker in the folder context menu tints the sidebar label and applies a colored background to folder items.</li>
  <li>Added auto-organize. The wand button near New Folder groups existing feeds into topic folders using a built-in keyword ruleset.</li>
  <li>Added feed sort mode. Feeds in the sidebar can now be sorted by name, unread count, or kept in the original custom order.</li>
</ul>
<h3>Changed</h3>
<ul>
  <li>Article titles in the reader panel are now links to the original URL. The external-link icon in the meta line shows an <kbd>o</kbd> shortcut hint on hover.</li>
  <li>The Feed/Full text mode selector is now a compact inline pill control, reducing vertical space. The <kbd>h</kbd> shortcut hint is in a hover tooltip.</li>
  <li>The article list is now virtualized. Scrolling through All Items with hundreds of articles no longer causes layout jank.</li>
  <li>The desktop breakpoint was lowered from 1024px to 768px so tablet-sized windows use the multi-panel layout.</li>
  <li>Publisher boilerplate phrases (&quot;Read more at&quot;, &quot;Continue reading&quot;, etc.) are stripped from feed content before display.</li>
</ul>
<h3>Fixed</h3>
<ul>
  <li>Fixed article text clipping at the right edge of the reader panel. Replaced Radix ScrollArea (which uses a <code>display:table</code> wrapper that breaks text wrapping) with a native <code>overflow-y-auto</code> container.</li>
  <li>Fixed mobile landing on the reader view when selecting a feed. The app now defaults to the article list, not the reader.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
  <entry>
    <id>feedzero:release:0.5.0</id>
    <title>v0.5.0 — Feed folders and mobile navigation</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-04-19T12:00:00Z</published>
    <updated>2026-04-19T12:00:00Z</updated>
    <summary>Organize feeds into collapsible folders, read all items in a folder at once, and swipe between articles on mobile.</summary>
    <content type="html"><![CDATA[<p>Organize feeds into collapsible folders, read all items in a folder at once, and swipe between articles on mobile.</p>
<h3>Added</h3>
<ul>
  <li>Added feed folders. Create folders from the sidebar, drag feeds between them, rename or delete folders from the context menu.</li>
  <li>Added folder aggregated feeds. Clicking a folder header shows all articles from every feed in that folder, with per-feed title and favicon on each row.</li>
  <li>Added swipe navigation on mobile. The article list and reader are side-by-side scroll-snap panels. Swipe left to read, swipe right to go back.</li>
  <li>Added a floating back pill on the mobile reader view. Tapping it scrolls back to the article list.</li>
  <li>Added safe-area inset support for iPhone Dynamic Island and notch. Content no longer renders under the notch in any orientation.</li>
  <li>Added inline feed rename from the three-dot context menu.</li>
  <li>Added Vercel Speed Insights for Core Web Vitals monitoring.</li>
</ul>
<h3>Changed</h3>
<ul>
  <li>The app now defaults to the All Items view when feeds exist, instead of auto-selecting the first feed.</li>
  <li>Unread counts are derived from a single source of truth (<code>articlesByFeedId</code>) instead of a separately stored counter. Badges update immediately after adding a feed from the Explore tab.</li>
  <li>All articles in a feed render at once. Removed the 25-article display cap and the Load More button.</li>
  <li>Sidebar action dots appear only on hover, not after clicking a feed. The fix uses <code>:focus-visible</code> so keyboard users still see the dots when tabbing.</li>
  <li>Article selection no longer shifts text. The left accent bar reserves its 2px space at all times.</li>
  <li>The sidebar was decomposed from a single 728-line component into focused sub-components (FeedItem, FolderItem, SidebarFeedList, confirmation dialogs).</li>
</ul>
<h3>Fixed</h3>
<ul>
  <li>Fixed unread badge not updating after adding a feed from the Explore tab.</li>
  <li>Fixed sidebar action dots and unread badge overlapping after clicking a feed.</li>
  <li>Fixed article text shifting 2px when selected.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
  <entry>
    <id>feedzero:release:0.4.0</id>
    <title>v0.4.0 — Release notes feed</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-04-06T18:00:00Z</published>
    <updated>2026-04-06T18:00:00Z</updated>
    <summary>The release notes are now an Atom feed that any RSS reader can subscribe to.</summary>
    <content type="html"><![CDATA[<p>The release notes are now an Atom feed that any RSS reader can subscribe to.</p>
<h3>Added</h3>
<ul>
  <li>Published the release notes as an Atom feed at feedzero.app/releases.xml. New installs auto-subscribe on first launch; existing users can subscribe via Settings → What's new.</li>
  <li>Added a &quot;Clear cached articles&quot; action to the feed context menu. It deletes the feed's stored articles and re-fetches from source. Read/unread status is lost, and older articles may not reappear if the source no longer publishes them.</li>
</ul>
<h3>Changed</h3>
<ul>
  <li>Article content now renders h1–h4 headings with appropriate sizing and spacing. Lists render with markers.</li>
  <li>Feeds in the sidebar sort alphabetically, with the release notes feed pinned to the top.</li>
  <li>Unread badges use a squircle shape with a subtle background.</li>
</ul>
<h3>Removed</h3>
<ul>
  <li>Removed the in-app changelog dialog (~700 lines of custom UI). The Atom feed replaces it.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
  <entry>
    <id>feedzero:release:0.3.1</id>
    <title>v0.3.1 — More vertical space</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-04-06T12:00:00Z</published>
    <updated>2026-04-06T12:00:00Z</updated>
    <summary>Removed the desktop header bar. Added per-feed unread counts and in-memory article preloading.</summary>
    <content type="html"><![CDATA[<p>Removed the desktop header bar. Added per-feed unread counts and in-memory article preloading.</p>
<h3>Added</h3>
<ul>
  <li>Added per-feed unread count badges in the sidebar. The count is derived from the full article set, not just the current page.</li>
  <li>Added source attribution in the reader panel. Each article shows the feed's favicon and title under the headline.</li>
  <li>Added a &quot;Load more&quot; button for feeds with more than 25 articles.</li>
</ul>
<h3>Changed</h3>
<ul>
  <li>Removed the 40-pixel desktop header bar. The sidebar toggle moved into the sidebar itself.</li>
  <li>Articles preload into memory at startup, so switching feeds no longer shows a loading state.</li>
  <li>Replaced the &quot;mark all read&quot; toolbar with a floating pill that appears at the bottom only when there are unread articles.</li>
  <li>Favicons now refresh on a 7-day cycle. Removed the manual &quot;Reload favicons&quot; menu item.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
  <entry>
    <id>feedzero:release:0.3.0</id>
    <title>v0.3.0 — Tracker and link-param stripping</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-04-06T06:00:00Z</published>
    <updated>2026-04-06T06:00:00Z</updated>
    <summary>Tracking pixels and URL tracking parameters are removed from feed content before it reaches the browser.</summary>
    <content type="html"><![CDATA[<p>Tracking pixels and URL tracking parameters are removed from feed content before it reaches the browser.</p>
<h3>Added</h3>
<ul>
  <li>Added tracking-pixel stripping. 1×1 images loaded from known tracker domains (Facebook, Google Analytics, Quantserve, Feedburner, and others) are removed from article HTML before render.</li>
  <li>Added URL tracking-parameter stripping. <code>utm_*</code>, <code>fbclid</code>, <code>gclid</code>, and around 20 similar parameters are removed from links inside article content.</li>
  <li>Added an anonymous feed catalog on the server. It records which feeds exist and how often they are fetched. No user-identifying information is stored.</li>
</ul>
<h3>Changed</h3>
<ul>
  <li>The changelog dialog supports arrow-key navigation between releases.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
  <entry>
    <id>feedzero:release:0.2.2</id>
    <title>v0.2.2 — Bug fixes</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-03-29T00:00:00Z</published>
    <updated>2026-03-29T00:00:00Z</updated>
    <summary>Fixes for favicon loading, feed refresh, and error messages.</summary>
    <content type="html"><![CDATA[<p>Fixes for favicon loading, feed refresh, and error messages.</p>
<h3>Fixed</h3>
<ul>
  <li>Fixed favicon loading for sites that serve icons at non-standard paths.</li>
  <li>Improved feed refresh reliability.</li>
  <li>Improved error messages when adding an invalid feed URL.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
  <entry>
    <id>feedzero:release:0.2.1</id>
    <title>v0.2.1 — Visual polish</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-03-28T00:00:00Z</published>
    <updated>2026-03-28T00:00:00Z</updated>
    <summary>Palette, transition, and typography adjustments.</summary>
    <content type="html"><![CDATA[<p>Palette, transition, and typography adjustments.</p>
<h3>Changed</h3>
<ul>
  <li>Warmed the background tint. Accent color is now blue-indigo.</li>
  <li>Added CSS transitions for hover, selection, and sidebar open/close.</li>
  <li>Reworked blockquotes, inline images, and editorial typography in article content.</li>
  <li>Unread and read article states are now distinguished by bold titles and an accent bar.</li>
  <li>Focus rings are softer, and the <code>prefers-reduced-motion</code> media query is honored.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
  <entry>
    <id>feedzero:release:0.2.0</id>
    <title>v0.2.0 — Explore, keyboard nav, sync, and OPML</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-03-28T00:00:00Z</published>
    <updated>2026-03-28T00:00:00Z</updated>
    <summary>A catalog of around 1,000 feeds, vim-style keyboard shortcuts, end-to-end encrypted sync, and OPML import/export.</summary>
    <content type="html"><![CDATA[<p>A catalog of around 1,000 feeds, vim-style keyboard shortcuts, end-to-end encrypted sync, and OPML import/export.</p>
<h3>Added</h3>
<ul>
  <li>Added an Explore tab with around 1,000 feeds organized by topic and country. The search box accepts a URL, which is added directly as a feed.</li>
  <li>Added vim-style keyboard navigation: <code>j</code>/<code>k</code> for next/previous article, <code>Enter</code> to add a feed, <code>Space</code> to scroll, <code>h</code> for full text view, <code>o</code> to open the original.</li>
  <li>Added unread dots and a &quot;mark all read&quot; action.</li>
  <li>Added optional end-to-end encrypted cloud sync. The encryption key is derived from a four-word passphrase generated from the EFF wordlist. Lost passphrase means lost data — by design.</li>
  <li>Added OPML import and export.</li>
</ul>
<h3>Changed</h3>
<ul>
  <li>Feed switching now reads from an in-memory cache instead of IndexedDB.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
  <entry>
    <id>feedzero:release:0.1.0</id>
    <title>v0.1.0 — First release</title>
    <link rel="alternate" href="https://feedzero.app/#releases" />
    <published>2026-01-31T00:00:00Z</published>
    <updated>2026-01-31T00:00:00Z</updated>
    <summary>The initial alpha.</summary>
    <content type="html"><![CDATA[<p>The initial alpha.</p>
<h3>Added</h3>
<ul>
  <li>Added RSS 2.0, Atom 1.0, and JSON Feed parsers.</li>
  <li>Added AES-256 encryption of all stored data in IndexedDB.</li>
  <li>Added optional cloud sync using a passphrase-derived encryption key.</li>
  <li>Added full-text article extraction for feeds that publish only summaries.</li>
  <li>Added dark mode.</li>
  <li>Added vim-style keyboard navigation.</li>
</ul>]]></content>
    <author><name>FeedZero</name></author>
  </entry>
</feed>
